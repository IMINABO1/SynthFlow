# PyTorch for RTX 5070 (Blackwell, sm_120): needs CUDA 12.8 (cu128) + torch>=2.7.
# The old cu124 build has no Blackwell kernels and fails at runtime on this GPU.
# Install with:  uv pip install -r requirements.txt --index-strategy unsafe-best-match
--extra-index-url https://download.pytorch.org/whl/cu128

# Deep learning (GPU)
torch==2.7.1+cu128
torchvision==0.22.1+cu128

# Core numerics / data
numpy
pandas
scipy
scikit-learn

# Plotting (EDA)
matplotlib
seaborn

# Training utilities
tqdm
wandb

# NOTE: transitive dependencies (sympy, networkx, filelock, jinja2, certifi, ...)
# are intentionally left unpinned so they resolve against torch 2.7's requirements.
# The previous file was a pip freeze from a torch-2.6 env and its pins conflicted.
